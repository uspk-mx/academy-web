packages:
  - "packages/*"  # For shared libraries
  - "apps/*"      # For applications

# Allow these packages to run build scripts during install (pnpm 10+ security feature)
onlyBuiltDependencies:
  - core-js
  - esbuild
  - msw
